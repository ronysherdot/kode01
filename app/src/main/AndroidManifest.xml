<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.budayaende">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/itb_stikom_bali"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/itb_stikom_bali"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Visit3Activity" android:screenOrientation="portrait"/>
        <activity android:name=".Visit2Activity" android:screenOrientation="portrait"/>
        <activity android:name=".Visit1Activity" android:screenOrientation="portrait"/>
        <activity android:name=".VisitActivity" android:screenOrientation="portrait"/>
        <activity android:name=".BudayaActivity" android:screenOrientation="portrait"/>
        <activity android:name=".FotoDetailActivity" android:screenOrientation="portrait"/>
        <activity android:name=".VideoActivity" android:screenOrientation="portrait"/>
        <activity android:name=".UpacaraActivity" android:screenOrientation="portrait"/>
        <activity android:name=".MusikActivity" android:screenOrientation="portrait"/>
        <activity android:name=".PakaianActivity" android:screenOrientation="portrait"/>
        <activity android:name=".RumahActivity" android:screenOrientation="portrait"/>
        <activity android:name=".TarianActivity" android:screenOrientation="portrait"/>
        <activity android:name=".GaleriActivity" android:screenOrientation="portrait"/>
        <activity android:name=".AboutActivity" android:screenOrientation="portrait"/>
        <activity android:name=".SejarahActivity" android:screenOrientation="portrait"/>
        <activity android:name=".MenuActivity" android:screenOrientation="portrait"/>
        <activity android:name=".MainActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>